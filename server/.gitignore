/target
/public/dist
/node_modules
# The generated client package and the OpenAPI document it is built from are build products;
# `.nextrs/template/` and `.nextrs/ensure-client.mjs` are the tracked sources they come from.
/.nextrs/client/
/.nextrs/openapi.json
.env
.vercel
.env*
