// Canonical challenge-creation URL. The legacy /admin/challenges route remains as a compatibility
// alias until existing bookmarks have had time to migrate.
export { default } from "../../admin/challenges/page";
